{
  "name": "functions",
  "description": "Firebase Cloud Functions for Google Calendar sync token exchange",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "googleapis": "^173.0.0",
    "node-fetch": "^2.7.0"
  },
  "private": true
}
